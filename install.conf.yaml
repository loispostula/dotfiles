- defaults:
    link:
      create: true
      relink: true

- clean: ['~',]


- link:
    ~/.config/systemd/user:
      path: config/systemd/user
      create: true
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.shell:
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc: vimrc
    ~/.zshrc:
    ~/.zsh:
    ~/.xinitrc: xinitrc
    ~/.emacs.d: emacs

- shell:
  - mkdir -p ~/.emails
  - mkdir -p ~/.cache/emacs
  - mkdir -p ~/.local/share/emacs
  - systemctl daemon-reload --user
  - systemctl enable --user ssh-agent && systemctl start --user ssh-agent
    #  - systemctl daemon-reload --user && systemctl enable --user gpg-agent && systemctl start --user gpg-agent
